/*
 * Salon Messages
 *
 * This contains all the text for the Salon container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SalonList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Salon list!',
  },
});
