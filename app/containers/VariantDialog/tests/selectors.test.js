// import { selectVariantDialogDomain } from '../selectors';

describe('selectVariantDialogDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
