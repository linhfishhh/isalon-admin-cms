// import { selectProductReviewDomain } from '../selectors';

describe('selectProductReviewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
