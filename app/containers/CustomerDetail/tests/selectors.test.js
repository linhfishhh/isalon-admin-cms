// import { selectCustomerDetailDomain } from '../selectors';

describe('selectCustomerDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
