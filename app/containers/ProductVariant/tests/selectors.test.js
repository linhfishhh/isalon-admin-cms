// import { selectProductVariantDomain } from '../selectors';

describe('selectProductVariantDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
