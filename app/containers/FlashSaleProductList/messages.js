/*
 * FlashSaleProductList Messages
 *
 * This contains all the text for the FlashSaleProduct container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FlashSaleProductList';

export default defineMessages({
  confirmDelete: {
    id: `${scope}.confirmDelete`,
    defaultMessage: 'Are you sure delete this product combo?',
  },
});
