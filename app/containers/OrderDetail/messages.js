/*
 * OrderDetail Messages
 *
 * This contains all the text for the OrderDetail container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OrderDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OrderDetail container!',
  },
});
