/*
 * ComboBox Messages
 *
 * This contains all the text for the Button components.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ComboBox';

export default defineMessages({
  addNewOption: {
    id: `${scope}.addNewOption`,
    defaultMessage: 'Add new option',
  },
});
